/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;

// next.config.js

module.exports = {
    images: {
      domains: ['fakestoreapi.com'], // Add this line
    },
  };
  
